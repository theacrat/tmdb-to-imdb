# Copy this file to .env and fill in your values
# .env is gitignored and will not be committed

# Required: TMDB API token (JWT bearer token)
TMDB_API="your-tmdb-api-token-here"

# Required: GCP Project ID
GCP_PROJECT_ID="tmdb-imdb-480800"

# Optional: Google application credentials (when using prod DB instead of emulator)
GOOGLE_APPLICATION_CREDENTIALS=""